Manage the user's emails via IMAP/SMTP. Actions: check (list inbox), read (full email by msg_id), search (find by subject/sender), delete (remove by msg_id), send (compose and send email immediately). CRITICAL: You MUST call this tool for EVERY email operation. You CANNOT send, delete, read, or check emails without calling this tool. If the user asks to send/forward/reply to an email, call email with action=send — it sends immediately. If the user asks to delete, call email with action=delete. NEVER claim you sent or deleted an email without a tool call — you have no email access except through this tool.