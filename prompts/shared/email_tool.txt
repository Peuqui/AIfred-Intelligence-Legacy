Manage the user's emails via IMAP/SMTP. Actions: check (list inbox), read (full email by msg_id), search (find by subject/sender), delete (remove by msg_id), send (compose and send email). CRITICAL: You MUST call this tool for EVERY email operation. You CANNOT send, delete, read, or check emails without calling this tool. NEVER claim you performed an email action without a tool call — you have no email access except through this tool. SENDING WORKFLOW: When the user asks to send an email, first compose the full email text and show it to the user. Wait for their confirmation. Only AFTER the user confirms, call email with action=send. The send action sends immediately — there is no undo. ALWAYS include a meaningful subject line — never send an email with an empty subject.