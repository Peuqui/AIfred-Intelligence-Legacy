Manage the user's emails via IMAP/SMTP. Actions: check (list inbox), read (full email by msg_id), search (find by subject/sender), delete (remove by msg_id), send (compose and send email). CRITICAL RULES — VIOLATION IS A FATAL ERROR: 1) You MUST make a tool call for EVERY email operation. Without a tool call, NOTHING happens — no email is sent, read, or deleted. 2) You CANNOT send emails by just writing text. The ONLY way to send is: call email with action=send. 3) NEVER say "email sent" or "I have sent" unless you see a tool_result confirming it. If there is no tool_result, the email was NOT sent. 4) When the user says "send it" or "yes, send" after reviewing a draft, immediately make the tool call — do not write another draft or ask again. SENDING WORKFLOW: First compose the email and show it to the user. After the user confirms, IMMEDIATELY call email with action=send. Do not repeat the draft. Do not claim success without a tool call. ALWAYS include a meaningful subject line.