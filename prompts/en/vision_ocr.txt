Extract ALL visible text, data and structures from the images as JSON. This is a complete OCR task.

CRITICAL - MULTIPLE IMAGES:
- If MULTIPLE images are provided, analyze EACH image INDIVIDUALLY and completely!
- Each image gets its own section in the JSON output
- Do NOT skip ANY image - ALL images must be analyzed!

CRITICAL RULES:
- Extract ONLY what is ACTUALLY visible in the images - NO additions, NO corrections, NO interpretations!
- DO NOT INVENT data or rows - if something is illegible, leave it empty or write "illegible"
- Copy text EXACTLY as shown - no spell corrections, no improvements
- For tables: Count the rows in the image and extract EXACTLY that number - no more, no less!

IMPORTANT:
- Capture ALL sections: headers, tables, lists, forms, plain text
- Extract EVERY row and EVERY column completely
- For multiple tables: Create an array with all tables
- For mixed content: Combine all structures in logical order
- Leave NOTHING out - complete document reconstruction!
- For MULTIPLE IMAGES: Each image as separate entry in "images" array!

Format for single structure:
```json
{
  "type": "table|list|form|text",
  "columns": ["Col1", "Col2"],
  "rows": [["Val1", "Val2"]]
}
```

Format for multiple structures (single image):
```json
{
  "type": "document",
  "sections": [
    {"type": "text", "content": "Header text"},
    {"type": "table", "columns": [...], "rows": [...]},
    {"type": "form", "fields": [...]}
  ]
}
```

Format for MULTIPLE IMAGES (IMPORTANT!):
```json
{
  "type": "multi_image",
  "images": [
    {
      "image_index": 1,
      "description": "Brief description of what is visible in the image",
      "content": {"type": "document", "sections": [...]}
    },
    {
      "image_index": 2,
      "description": "Brief description of what is visible in the second image",
      "content": {"type": "table", "columns": [...], "rows": [...]}
    }
  ]
}
```

Types:
- table: {"columns": [...], "rows": [[...]]} - Tables
- list: {"items": [...]} - Lists
- form: {"fields": [{"label": "...", "value": "..."}]} - Forms
- text: {"content": "..."} - Plain text
- document: {"sections": [...]} - Multiple sections

Respond ONLY with valid JSON in markdown code block!
