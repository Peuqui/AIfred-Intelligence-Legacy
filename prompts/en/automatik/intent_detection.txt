Analyze this user request:

"{user_query}"

**1. Intent:**
- FACTUAL: Facts, explanations, definitions, news, questions
- CREATIVE: Stories, poems, creative writing, brainstorming
- MIXED: Combination of both

**2. Addressee (who should respond):**
Available agents with their specializations:
{agent_list}

Select an addressee in TWO cases:
A) **By name:** User directly addresses an agent ("Hey Sokrates", "Codi, hilf mir")
B) **By expertise:** The query clearly matches ONE specialist's domain — not general knowledge.

Rules for name detection:
- Speaking TO someone = addressee. Speaking ABOUT someone = NOT addressee.
- Ignore grammar/spelling — focus on semantic intent.

Rules for expertise routing:
- Only route if the query CLEARLY matches ONE specialist's description above.
- General questions, small talk, time/date, weather → Leave EMPTY (defaults to aifred).
- If multiple agents could handle it → Leave EMPTY (defaults to aifred).
- Agents without a description → Only selectable by name, never by expertise.

If unsure → Leave EMPTY (use || in output). aifred handles everything by default.

**3. Language of the user text in quotes above:**
- DE: The text is in German (Deutsch)
- EN: The text is in English

Detect the language of the QUOTED USER TEXT only, not these instructions.
Examples: "Wie geht es dir?" → DE | "How are you?" → EN | "Was kostet das?" → DE

**Output format - STRICT SCHEMA:**

Return EXACTLY this format: INTENT|ADDRESSEE|LANGUAGE

Rules:
- INTENT must be: FACTUAL, CREATIVE, or MIXED (uppercase)
- ADDRESSEE must be: one of the agent IDs listed above, or EMPTY (lowercase, no quotes)
- LANGUAGE must be: DE or EN (uppercase)
- Use | as separator (pipe character)
- If no addressee: leave empty between pipes (example: FACTUAL||DE)
- No explanations, no extra text, ONLY the three fields
