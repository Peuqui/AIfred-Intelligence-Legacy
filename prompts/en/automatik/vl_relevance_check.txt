The user previously uploaded images in this conversation.

Images discussed:
{image_context}

Recent conversation (latest first):
{recent_context}

The user now asks:
"{user_query}"

Does this new question require looking at a previously uploaded image to answer?

**Output format - STRICT SCHEMA:**

Return EXACTLY one of:
- IMAGE:N (where N is the image number, 1-indexed)
- NONE

Rules:
- IMAGE:N only if the question DIRECTLY asks about the visual content of that image (e.g., "what does the image show?", "read the text on the document", "who is in the photo?")
- NONE if the question can be answered without looking at any image — even if the topic is loosely related
- NONE if the recent conversation has shifted to a different topic (e.g., coding, general questions)
- When in doubt → NONE
- No explanations, no extra text, ONLY the result
